query userGetByEmail($email: String!) {
    userGetByEmail(user:{
        email: $email

    })
    {
        user{
            id
            firstName
            lastName
            address
            email
            phone
            role
            lastLogin
            language
        }
    }
}