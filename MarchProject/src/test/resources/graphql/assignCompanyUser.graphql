mutation assignCompanyUser($id: ID!, $users: [UserIdentInput!]!){
    assignCompanyUser(company:{
        id: $id
        users: $users
    })
    {
        company {
            id
            users {
                id
            }
        }
    }
}


