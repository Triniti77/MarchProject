query companyGetByUser($users: [UserIdentInput!]!){
    companyGetByUser(company:{
        users: $users
    })
    {
        user{
            id
            companies{
                id
            }
        }
    }
}